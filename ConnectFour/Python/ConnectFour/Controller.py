import os
import random
import time
from abc import ABCMeta, abstractmethod

import Point


class Controller: 

    model=Model()
    whosGo=false 
    
    def __init__(self):
        self

    def setup(model):
        this.model = model;    

    def playermove(point):
        model.movePlayer(point);
     
 